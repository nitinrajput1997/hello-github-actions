name: "Hello Actions"
description: "Greet someone"
author: "nitinrajput706021.com"

inputs:
  MY_NAME:
    description: "Who to greet"
    required: true
    default: "Nitin"

runs:
  using: "docker"
  image: "Dockerfile"

branding:
  icon: "mic"
  color: "purple"
